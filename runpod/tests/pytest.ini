[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: exercises real RT-DETR weights + real video decode (slow)
    unit: no heavy runtime deps required
addopts = -ra --tb=short
