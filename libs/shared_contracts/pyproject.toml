[project]
name = "shared-contracts"
version = "0.1.0"
description = "GreyEye shared Pydantic models, enums, and event schemas"
requires-python = ">=3.11"

dependencies = [
    "cryptography>=42.0",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
