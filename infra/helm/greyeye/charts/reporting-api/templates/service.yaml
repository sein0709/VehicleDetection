apiVersion: v1
kind: Service
metadata:
  name: {{ include "reporting-api.fullname" . }}
  labels:
    {{- include "reporting-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reporting-api.selectorLabels" . | nindent 4 }}
